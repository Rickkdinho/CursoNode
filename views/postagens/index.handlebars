<div class="card">
    <div class="card-body">
        <h1>{{postagens.titulo}}</h1>
        <hr>
        <small>Data de publicação: {{postagens.date}}</small><br>
        <hr>
        {{postagens.conteudo}}
    </div>
</div>
